{
  "name": "@aster/persona-contract-compiler",
  "version": "1.0.0",
  "private": false,
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/packages/core/src/index.js",
  "types": "./dist/packages/core/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/packages/core/src/index.d.ts",
      "default": "./dist/packages/core/src/index.js"
    },
    "./contracts/*": "./packages/contracts/*"
  },
  "files": [
    "dist/packages",
    "dist/apps",
    "packages/contracts",
    "examples",
    "db/migrations",
    "scripts/migrate-postgres.ts",
    "README.md",
    "LICENSE"
  ],
  "packageManager": "pnpm@10.30.2",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.ts",
    "test:postgres": "node --test tests/postgres-store.test.ts tests/migrations.test.ts",
    "test:compiled": "pnpm run build && node --test dist/tests/*.test.js",
    "verify": "pnpm run check:private-boundary && pnpm run build && pnpm test && pnpm run test:compiled && pnpm pack --dry-run",
    "db:migrate": "node scripts/migrate-postgres.ts",
    "check:private-boundary": "node scripts/check-private-boundary.mjs",
    "lint": "pnpm run check:private-boundary && pnpm run build"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "@types/pg": "^8.20.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
